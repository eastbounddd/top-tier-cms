"use client";
import { useState } from "react";
export function WriterManager(){const [form,setForm]=useState({display_name:"",email:"",password:"",role:"writer"});const [message,setMessage]=useState("");const submit=async(e:React.FormEvent)=>{e.preventDefault();setMessage("Creating account…");const r=await fetch("/api/admin/writers",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const j=await r.json();setMessage(r.ok?"Writer account created.":j.error);if(r.ok)setForm({display_name:"",email:"",password:"",role:"writer"});};return <form className="panel writer-form" onSubmit={submit}><h2>Create Writer Login</h2><label>Name<input required value={form.display_name} onChange={e=>setForm({...form,display_name:e.target.value})}/></label><label>Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Temporary Password<input required minLength={8} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><label>Role<select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option value="writer">Writer</option><option value="editor">Editor</option><option value="owner">Owner</option></select></label><button className="red-button">Create Account</button>{message&&<p>{message}</p>}</form>}
